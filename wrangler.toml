# Cloudflare Pages config for obs-unified-docs.
# Used by `wrangler pages deploy` and by Cloudflare's GitHub integration.

name = "obsunified-docs"
pages_build_output_dir = "./build/client"
compatibility_date = "2026-05-01"
